<?php

namespace App\Http\Controllers\Auth;

use App\Http\Controllers\ApiController;

class AuthController extends ApiController
{
    /**
     * Example authentication controller extending ApiController
     * 
     * All API controllers should:
     * 1. Extend ApiController instead of Controller
     * 2. Use the ApiResponse trait methods for responses
     * 
     * Examples:
     * - return $this->success($data, 'User created');
     * - return $this->created($data, 'Account registered');
     * - return $this->error('Something went wrong', 400);
     * - return $this->validationError($errors);
     * - return $this->notFound();
     * - return $this->unauthorized();
     * - return $this->paginated($users);
     */
}
